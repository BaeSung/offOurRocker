// Re-export types and configs from shared types for backward compatibility
export { STATUS_CONFIG, GENRE_CONFIG } from '../../shared/types'
export type { WorkStatus, Genre, Work, Chapter, Series } from '../../shared/types'
